# 1.3 Миграция: обновление таблицы `agents` + таблицы инструментов и MCP

**Sprint:** 1 — Новые модели данных и миграции
**Файл миграции:** `backend/db/migrations/016_alter_agents.sql`
**Go-модели:** обновление `workflow.go` (Agent), новые `tool_definition.go`, `mcp_server_config.go`
**Статус:** ⬜ Не начата

---

## Описание

Расширить таблицу `agents` новыми полями и создать 4 дополнительные таблицы для управления инструментами и MCP-серверами агентов. Вместо JSONB-массивов строк используем нормализованные таблицы с bindings — это даёт валидацию, переиспользуемость, discoverability для UI и безопасное хранение credentials.

---

## Схема связей

```
agents (004, расширяется в 016)
    │
    ├──> teams (FK: team_id, nullable)
    │
    ├──< agent_tool_bindings (M:N)
    │       └──> tool_definitions
    │
    └──< agent_mcp_bindings (M:N)
            └──> mcp_server_configs
                    └──> projects (FK: project_id)
```

---

## Часть 1: ALTER TABLE agents

### Новые колонки

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `team_id` | UUID | YES | — | FK → teams ON DELETE SET NULL |
| `model` | VARCHAR(255) | YES | — | LLM модель (claude-sonnet-4-20250514, gpt-4o) |
| `skills` | JSONB | NO | `'[]'` | Теги навыков (влияют на промпт) |
| `code_backend` | VARCHAR(50) | YES | — | `claude-code` / `aider` / `custom` |
| `settings` | JSONB | NO | `'{}'` | Доп. настройки (temperature, max_tokens) |

### Изменения constraints

| Действие | Описание |
|----------|----------|
| ADD CHECK | `role` — 8 допустимых значений (старые + новые) |
| ADD CHECK | `code_backend` — NULL или `claude-code`/`aider`/`custom` |
| DROP UNIQUE | Глобальный `agents_name_key` (из миграции 010) |
| ADD UNIQUE INDEX | Partial `(team_id, name) WHERE team_id IS NOT NULL` |

### Что НЕ добавляется в agents (заменено таблицами)

- ~~`tools JSONB`~~ → `agent_tool_bindings` + `tool_definitions`
- ~~`mcp_servers JSONB`~~ → `agent_mcp_bindings` + `mcp_server_configs`

---

## Часть 2: Таблица `tool_definitions`

Реестр всех доступных инструментов. Загружается из YAML при старте (как промпты).

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `id` | UUID | NO | `gen_random_uuid()` | PK |
| `name` | VARCHAR(255) | NO | — | UNIQUE, идентификатор (`create_task`, `vector_search`) |
| `description` | TEXT | NO | — | Описание для LLM (передаётся в function calling) |
| `category` | VARCHAR(100) | NO | — | Группировка: `task_management`, `code`, `search`, `review` |
| `parameters_schema` | JSONB | NO | `'{}'` | JSON Schema параметров для LLM tool calling |
| `is_builtin` | BOOL | NO | `true` | true = встроенный (из YAML), false = пользовательский |
| `is_active` | BOOL | NO | `true` | Вкл/выкл |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |
| `updated_at` | TIMESTAMPTZ | NO | `NOW()` | |

---

## Часть 3: Таблица `agent_tool_bindings`

Many-to-many связь Agent <-> ToolDefinition.

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `agent_id` | UUID | NO | — | FK → agents ON DELETE CASCADE |
| `tool_definition_id` | UUID | NO | — | FK → tool_definitions ON DELETE CASCADE |
| `config` | JSONB | NO | `'{}'` | Переопределение настроек tool для этого агента |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |

Composite PK: `(agent_id, tool_definition_id)`

---

## Часть 4: Таблица `mcp_server_configs`

Конфигурации MCP-серверов, привязанные к проекту.

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `id` | UUID | NO | `gen_random_uuid()` | PK |
| `project_id` | UUID | NO | — | FK → projects ON DELETE CASCADE |
| `name` | VARCHAR(255) | NO | — | Имя для UI ("GitHub MCP", "PostgreSQL MCP") |
| `url` | VARCHAR(1024) | NO | — | URL MCP-сервера |
| `auth_type` | VARCHAR(50) | NO | `'none'` | `none` / `api_key` / `oauth` / `bearer` |
| `encrypted_credentials` | BYTEA | YES | — | Зашифрованные auth данные (AES-256-GCM) |
| `settings` | JSONB | NO | `'{}'` | Доп. настройки (headers, timeout, retry) |
| `is_active` | BOOL | NO | `true` | Вкл/выкл |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |
| `updated_at` | TIMESTAMPTZ | NO | `NOW()` | |

UNIQUE: `(project_id, name)` — одно имя MCP на проект.

---

## Часть 5: Таблица `agent_mcp_bindings`

Many-to-many связь Agent <-> MCPServerConfig.

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `agent_id` | UUID | NO | — | FK → agents ON DELETE CASCADE |
| `mcp_server_config_id` | UUID | NO | — | FK → mcp_server_configs ON DELETE CASCADE |
| `settings` | JSONB | NO | `'{}'` | Переопределение настроек для этого агента |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |

Composite PK: `(agent_id, mcp_server_config_id)`

---

## SQL миграция

### UP

```sql
-- +goose Up
-- +goose StatementBegin

-- === 1. ALTER agents: новые колонки ===
ALTER TABLE agents ADD COLUMN team_id UUID REFERENCES teams(id) ON DELETE SET NULL;
ALTER TABLE agents ADD COLUMN model VARCHAR(255);
ALTER TABLE agents ADD COLUMN skills JSONB DEFAULT '[]';
ALTER TABLE agents ADD COLUMN code_backend VARCHAR(50);
ALTER TABLE agents ADD COLUMN settings JSONB DEFAULT '{}';

ALTER TABLE agents ADD CONSTRAINT chk_agents_code_backend
    CHECK (code_backend IS NULL OR code_backend IN ('claude-code', 'aider', 'custom'));
ALTER TABLE agents ADD CONSTRAINT chk_agents_role
    CHECK (role IN ('worker', 'supervisor', 'orchestrator', 'planner', 'developer', 'reviewer', 'tester', 'devops'));

ALTER TABLE agents DROP CONSTRAINT agents_name_key;
CREATE UNIQUE INDEX idx_agents_team_name ON agents(team_id, name) WHERE team_id IS NOT NULL;
CREATE INDEX idx_agents_team_id ON agents(team_id);
CREATE INDEX idx_agents_role ON agents(role);

-- === 2. tool_definitions ===
CREATE TABLE tool_definitions (
    id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name              VARCHAR(255) NOT NULL UNIQUE,
    description       TEXT NOT NULL,
    category          VARCHAR(100) NOT NULL,
    parameters_schema JSONB DEFAULT '{}',
    is_builtin        BOOLEAN NOT NULL DEFAULT true,
    is_active         BOOLEAN NOT NULL DEFAULT true,
    created_at        TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at        TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_tool_definitions_category ON tool_definitions(category);

-- === 3. agent_tool_bindings ===
CREATE TABLE agent_tool_bindings (
    agent_id           UUID NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
    tool_definition_id UUID NOT NULL REFERENCES tool_definitions(id) ON DELETE CASCADE,
    config             JSONB DEFAULT '{}',
    created_at         TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    PRIMARY KEY (agent_id, tool_definition_id)
);

-- === 4. mcp_server_configs ===
CREATE TABLE mcp_server_configs (
    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id            UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    name                  VARCHAR(255) NOT NULL,
    url                   VARCHAR(1024) NOT NULL,
    auth_type             VARCHAR(50) NOT NULL DEFAULT 'none',
    encrypted_credentials BYTEA,
    settings              JSONB DEFAULT '{}',
    is_active             BOOLEAN NOT NULL DEFAULT true,
    created_at            TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at            TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT chk_mcp_auth_type CHECK (auth_type IN ('none', 'api_key', 'oauth', 'bearer')),
    CONSTRAINT uq_mcp_project_name UNIQUE (project_id, name)
);

CREATE INDEX idx_mcp_server_configs_project_id ON mcp_server_configs(project_id);

-- === 5. agent_mcp_bindings ===
CREATE TABLE agent_mcp_bindings (
    agent_id             UUID NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
    mcp_server_config_id UUID NOT NULL REFERENCES mcp_server_configs(id) ON DELETE CASCADE,
    settings             JSONB DEFAULT '{}',
    created_at           TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    PRIMARY KEY (agent_id, mcp_server_config_id)
);

-- +goose StatementEnd
```

### DOWN

```sql
-- +goose Down
-- +goose StatementBegin

DROP TABLE IF EXISTS agent_mcp_bindings;
DROP INDEX IF EXISTS idx_mcp_server_configs_project_id;
DROP TABLE IF EXISTS mcp_server_configs;
DROP TABLE IF EXISTS agent_tool_bindings;
DROP INDEX IF EXISTS idx_tool_definitions_category;
DROP TABLE IF EXISTS tool_definitions;

DROP INDEX IF EXISTS idx_agents_role;
DROP INDEX IF EXISTS idx_agents_team_id;
DROP INDEX IF EXISTS idx_agents_team_name;
ALTER TABLE agents ADD CONSTRAINT agents_name_key UNIQUE (name);

ALTER TABLE agents DROP CONSTRAINT IF EXISTS chk_agents_role;
ALTER TABLE agents DROP CONSTRAINT IF EXISTS chk_agents_code_backend;

ALTER TABLE agents DROP COLUMN IF EXISTS settings;
ALTER TABLE agents DROP COLUMN IF EXISTS code_backend;
ALTER TABLE agents DROP COLUMN IF EXISTS skills;
ALTER TABLE agents DROP COLUMN IF EXISTS model;
ALTER TABLE agents DROP COLUMN IF EXISTS team_id;

-- +goose StatementEnd
```

---

## Зависимости

**Требует:** agents (004), teams (015), projects (014)
**Зависят от этой:** Sprint 2 (CRUD API), Sprint 6 (Orchestrator)

---

## Критерии приёмки

- [ ] Файл `016_alter_agents.sql` создан
- [ ] UP и DOWN работают
- [ ] Тест UP → DOWN → UP проходит без ошибок
- [ ] 5 новых колонок в agents (team_id, model, skills, code_backend, settings)
- [ ] CHECK constraints: role (8 значений), code_backend (NULL + 3 значения), mcp auth_type (4 значения)
- [ ] Глобальный UNIQUE name убран, partial index (team_id, name) работает
- [ ] Таблица tool_definitions создана, UNIQUE name работает
- [ ] Таблица agent_tool_bindings создана, composite PK, CASCADE работает
- [ ] Таблица mcp_server_configs создана, UNIQUE (project_id, name) работает
- [ ] Таблица agent_mcp_bindings создана, composite PK, CASCADE работает
- [ ] Обратная совместимость: существующие агенты (role=worker/supervisor) не ломаются
- [ ] Go-модели созданы/обновлены (Agent, ToolDefinition, MCPServerConfig, AgentToolBinding, AgentMCPBinding)
- [ ] Enum-типы: AgentRole, CodeBackend, MCPAuthType с IsValid()
- [ ] `go build ./...` и `go test ./...` проходят
