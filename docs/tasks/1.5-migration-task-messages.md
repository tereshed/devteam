# 1.5 Миграция: таблица `task_messages`

**Sprint:** 1 — Новые модели данных и миграции
**Файл миграции:** `backend/db/migrations/018_create_task_messages.sql`
**Go-модель:** `backend/internal/models/task_message.go`
**Статус:** ⬜ Не начата

---

## Описание

Создать таблицу `task_messages` — лог коммуникации между агентами (и пользователем) в контексте конкретной задачи. Каждое сообщение фиксирует: кто отправил (user/agent), тип сообщения (instruction, result, question, feedback, error), содержимое и метаданные (tokens_used, model, duration_ms). Используется для аудита, отладки и индексации в векторную БД.

---

## Схема связей

```
task_messages
  ├──> tasks (FK: task_id, NOT NULL, ON DELETE CASCADE)
  └──> users/agents (полиморфная связь: sender_type + sender_id, без FK)
```

---

## Таблица `task_messages`

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `id` | UUID | NO | `gen_random_uuid()` | PK |
| `task_id` | UUID | NO | — | FK → tasks ON DELETE CASCADE |
| `sender_type` | VARCHAR(50) | NO | — | `user` / `agent` |
| `sender_id` | UUID | NO | — | ID отправителя (полиморфная связь) |
| `content` | TEXT | NO | — | Содержимое сообщения |
| `message_type` | VARCHAR(50) | NO | — | Тип сообщения (см. таблицу ниже) |
| `metadata` | JSONB | NO | `'{}'` | Доп. данные (tokens_used, model, duration_ms) |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |

---

## Типы сообщений

| Тип | Описание | Типичный отправитель |
|-----|----------|---------------------|
| `instruction` | Задание / указание агенту | Orchestrator, Planner, User |
| `result` | Результат выполнения | Developer, Tester |
| `question` | Уточняющий вопрос | Любой агент |
| `feedback` | Обратная связь (review, corrections) | Reviewer, User |
| `error` | Сообщение об ошибке | Любой агент |

---

## Индексы

| Индекс | Тип | Колонки | Описание |
|--------|-----|---------|----------|
| `task_messages_pkey` | PK | `id` | |
| `idx_task_messages_task_id` | INDEX | `task_id` | Сообщения задачи |
| `idx_task_messages_sender` | INDEX | `(sender_type, sender_id)` | Сообщения по отправителю |
| `idx_task_messages_message_type` | INDEX | `message_type` | Фильтр по типу |
| `idx_task_messages_task_created` | INDEX | `(task_id, created_at)` | Хронологический лог задачи |

---

## CHECK constraints

| Constraint | Условие |
|------------|---------|
| `chk_task_messages_sender_type` | `sender_type IN ('user', 'agent')` |
| `chk_task_messages_message_type` | `message_type IN ('instruction', 'result', 'question', 'feedback', 'error')` |

---

## Полиморфная связь `sender_type` + `sender_id`

Аналогично `created_by_type` + `created_by_id` в таблице `tasks` — FK на уровне БД не ставим, валидация в service-слое.

---

## SQL миграция

### UP

```sql
-- +goose Up
-- +goose StatementBegin

CREATE TABLE task_messages (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id       UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    sender_type   VARCHAR(50) NOT NULL,
    sender_id     UUID NOT NULL,
    content       TEXT NOT NULL,
    message_type  VARCHAR(50) NOT NULL,
    metadata      JSONB NOT NULL DEFAULT '{}',
    created_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW(),

    CONSTRAINT chk_task_messages_sender_type CHECK (sender_type IN ('user', 'agent')),
    CONSTRAINT chk_task_messages_message_type CHECK (message_type IN (
        'instruction', 'result', 'question', 'feedback', 'error'
    ))
);

CREATE INDEX idx_task_messages_task_id ON task_messages(task_id);
CREATE INDEX idx_task_messages_sender ON task_messages(sender_type, sender_id);
CREATE INDEX idx_task_messages_message_type ON task_messages(message_type);
CREATE INDEX idx_task_messages_task_created ON task_messages(task_id, created_at);

-- +goose StatementEnd
```

### DOWN

```sql
-- +goose Down
-- +goose StatementBegin

DROP INDEX IF EXISTS idx_task_messages_task_created;
DROP INDEX IF EXISTS idx_task_messages_message_type;
DROP INDEX IF EXISTS idx_task_messages_sender;
DROP INDEX IF EXISTS idx_task_messages_task_id;
DROP TABLE IF EXISTS task_messages;

-- +goose StatementEnd
```

---

## Go-модель

```go
package models

type SenderType string

const (
    SenderTypeUser  SenderType = "user"
    SenderTypeAgent SenderType = "agent"
)

type MessageType string

const (
    MessageTypeInstruction MessageType = "instruction"
    MessageTypeResult      MessageType = "result"
    MessageTypeQuestion    MessageType = "question"
    MessageTypeFeedback    MessageType = "feedback"
    MessageTypeError       MessageType = "error"
)

type TaskMessage struct {
    ID          uuid.UUID      `gorm:"..." json:"id"`
    TaskID      uuid.UUID      `gorm:"..." json:"task_id"`
    Task        *Task          `gorm:"foreignKey:TaskID" json:"task,omitempty"`
    SenderType  SenderType     `gorm:"..." json:"sender_type"`
    SenderID    uuid.UUID      `gorm:"..." json:"sender_id"`
    Content     string         `gorm:"..." json:"content"`
    MessageType MessageType    `gorm:"..." json:"message_type"`
    Metadata    datatypes.JSON `gorm:"..." json:"metadata"`
    CreatedAt   time.Time      `gorm:"..." json:"created_at"`
}
```

---

## Пример metadata

```json
{
  "tokens_used": 1523,
  "model": "claude-sonnet-4-20250514",
  "duration_ms": 4200,
  "prompt_tokens": 850,
  "completion_tokens": 673
}
```

---

## Зависимости

**Требует:** tasks (017)
**Зависят от этой:** Sprint 2+ (Task Messages API), Sprint 6 (Orchestrator — коммуникация агентов)

---

## Критерии приёмки

- [ ] Файл `018_create_task_messages.sql` создан
- [ ] UP и DOWN работают
- [ ] Тест UP → DOWN → UP проходит без ошибок
- [ ] Таблица `task_messages` создана с 8 колонками
- [ ] FK: `task_id` → tasks ON DELETE CASCADE
- [ ] CHECK: `sender_type` (2 значения), `message_type` (5 значений)
- [ ] 4 индекса (task_id, sender, message_type, task+created_at)
- [ ] Полиморфная связь `sender_type` + `sender_id` без FK
- [ ] Go-модель `TaskMessage` создана с enum-типами `SenderType`, `MessageType` и `IsValid()`
- [ ] `go build ./...` и `go test ./...` проходят
