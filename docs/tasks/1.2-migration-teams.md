# 1.2 Миграция: таблица `teams`

**Sprint:** 1 — Новые модели данных и миграции
**Файл миграции:** `backend/db/migrations/015_create_teams.sql`
**Go-модель:** `backend/internal/models/team.go`
**Статус:** ✅ Выполнена

---

## Описание

Создать таблицу `teams` — связующее звено между проектом и агентами. Каждый проект имеет одну команду (пока тип только `development`), к которой привязываются AI-агенты.

---

## Связь между таблицами

```
projects (014)
    │
    └──< teams (FK: project_id, 1:1 пока)
            │
            └──< agents (016, FK: team_id, 1:N)
                    Orchestrator
                    Planner
                    Developer
                    Reviewer
                    Tester
```

Текущая задача создаёт только `teams`. Привязка `agents.team_id` добавляется в задаче 1.3 (`016_alter_agents.sql`).

---

## Схема таблицы

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `id` | UUID | NO | `gen_random_uuid()` | PK |
| `name` | VARCHAR(255) | NO | — | Название команды |
| `project_id` | UUID | NO | — | FK → projects, UNIQUE (1 команда на проект) |
| `type` | VARCHAR(50) | NO | `'development'` | Тип команды |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |
| `updated_at` | TIMESTAMPTZ | NO | `NOW()` | |

---

## Детали полей

### `name` (VARCHAR 255, NOT NULL)
- Название команды, отображается в UI
- По умолчанию при создании проекта генерируется автоматически: `"Development Team"`
- Пользователь может переименовать

### `project_id` (UUID, NOT NULL, FK → projects, UNIQUE)
- Каждый проект имеет ровно одну команду
- UNIQUE constraint гарантирует соотношение 1:1
- ON DELETE CASCADE — при удалении проекта команда удаляется

### `type` (VARCHAR 50, NOT NULL, default `'development'`)
- Тип команды
- Пока единственное значение: `development`
- Зарезервировано для будущих типов: `design`, `qa`, `devops`
- CHECK constraint на уровне БД

---

## SQL миграция

### UP

```sql
-- +goose Up
-- +goose StatementBegin
CREATE TABLE teams (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name        VARCHAR(255) NOT NULL,
    project_id  UUID NOT NULL UNIQUE REFERENCES projects(id) ON DELETE CASCADE,
    type        VARCHAR(50) NOT NULL DEFAULT 'development',
    created_at  TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at  TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT chk_teams_type CHECK (type IN ('development'))
);

CREATE INDEX idx_teams_project_id ON teams(project_id);
-- +goose StatementEnd
```

### DOWN

```sql
-- +goose Down
-- +goose StatementBegin
DROP INDEX IF EXISTS idx_teams_project_id;
DROP TABLE IF EXISTS teams;
-- +goose StatementEnd
```

---

## Go-модель

```go
package models

import (
    "time"

    "github.com/google/uuid"
    "gorm.io/gorm"
)

type TeamType string

const (
    TeamTypeDevelopment TeamType = "development"
)

func (tt TeamType) IsValid() bool {
    switch tt {
    case TeamTypeDevelopment:
        return true
    default:
        return false
    }
}

type Team struct {
    ID        uuid.UUID `gorm:"type:uuid;primaryKey;default:gen_random_uuid()" json:"id"`
    Name      string    `gorm:"type:varchar(255);not null" json:"name"`
    ProjectID uuid.UUID `gorm:"type:uuid;not null;uniqueIndex" json:"project_id"`
    Project   *Project  `gorm:"foreignKey:ProjectID" json:"project,omitempty"`
    Type      TeamType  `gorm:"type:varchar(50);not null;default:'development'" json:"type"`
    CreatedAt time.Time `gorm:"type:timestamp with time zone;default:now()" json:"created_at"`
    UpdatedAt time.Time `gorm:"type:timestamp with time zone;default:now()" json:"updated_at"`
}

func (Team) TableName() string {
    return "teams"
}

func (t *Team) BeforeCreate(tx *gorm.DB) error {
    if t.ID == uuid.Nil {
        t.ID = uuid.New()
    }
    return nil
}
```

---

## Тестирование миграции

```bash
# 1. Накатить
~/go/bin/goose -dir db/migrations postgres "host=localhost port=5433 user=yugabyte password=yugabyte dbname=yugabyte sslmode=disable" up

# 2. Проверить таблицу
docker exec -it wibe_yugabytedb /home/yugabyte/bin/ysqlsh -h 127.0.0.1 -U yugabyte -c "\d teams"

# 3. CHECK constraint — невалидный type
docker exec -it wibe_yugabytedb /home/yugabyte/bin/ysqlsh -h 127.0.0.1 -U yugabyte -c "
  INSERT INTO teams (name, project_id, type) VALUES ('test', gen_random_uuid(), 'invalid');
"
-- Ожидание: ERROR (chk_teams_type)

# 4. UNIQUE constraint — две команды для одного проекта
-- (создать проект, создать 2 teams с одним project_id → ERROR)

# 5. ON DELETE CASCADE — удаление проекта удаляет команду

# 6. Откатить
~/go/bin/goose -dir db/migrations postgres "..." down

# 7. Снова накатить
~/go/bin/goose -dir db/migrations postgres "..." up
```

---

## Зависимости

**Требует:** таблица `projects` (миграция 014)
**Зависят от этой:** миграция 016 (`alter agents`, добавит FK `team_id → teams`)

---

## Критерии приёмки

- [x] Файл `015_create_teams.sql` создан
- [x] `-- +goose Up` и `-- +goose Down` работают
- [x] Тест UP → DOWN → UP проходит без ошибок
- [x] CHECK constraint на `type` работает (`'invalid'` → ERROR chk_teams_type)
- [x] UNIQUE constraint на `project_id` работает (дубликат → ERROR teams_project_id_key)
- [x] ON DELETE CASCADE проверен (удаление project → teams_remaining: 0)
- [x] Go-модель `team.go` создана с relation на Project (`foreignKey:ProjectID`)
- [x] Enum-тип `TeamType` с валидацией (`IsValid()`)
- [x] Компиляция `go build ./...` и `go vet` проходят
