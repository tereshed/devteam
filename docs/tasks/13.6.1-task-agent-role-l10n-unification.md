# 13.6.1 Унификация строк ролей агента (`taskAgentRole*` → `agentRole*`)

**Sprint:** 13 — продолжение после [13.6 Локализация / аудит l10n для Sprint 13 (ru, en)](./13.6-l10n-sprint13-arb.md)  
**Статус:** ⬜ (в [README](../../README.md) в таблицу Sprint 13 строку добавлять только после принятия в бэклог)

Эта задача **не** входит в скоуп [13.6](./13.6-l10n-sprint13-arb.md): там только аудит дельты Sprint 13 и согласование префиксов для **новых** ключей. Переименование девяти ключей `taskAgentRole*` затрагивает Sprint 12 (`task_detail_screen`, `task_status_card`), ARB, все call-site’ы и виджет-тесты — отдельный объём и отдельный PR.

---

## Зачем выносить из 13.6

- В ARB уже живут `taskAgentRole*` (роли в контексте задач); экран команды использует `team*` и общие хелперы отображения.
- Полная миграция требует: новые ключи `agentRole*` в `app_ru.arb` / `app_en.arb`, удаление старых (или явное решение про алиасы — только один вариант в PR), правки `frontend/lib/` и `frontend/test/`, `make frontend-codegen`.
- Общий хелпер отображения ролей без дублирования — **код** (domain/presentation), не l10n; смешивание с чисто ARB-PR 13.6 раздувает ответственность.

---

## Приёмка (черновик)

- [ ] `rg -n 'taskAgentRole' frontend/lib frontend/test frontend/lib/l10n` — пусто **или** только в комментариях миграции (явно оговорить в PR).
- [ ] `make frontend-l10n-check`, `make frontend-codegen`, `make frontend-analyze`, `make frontend-test` — успех.
