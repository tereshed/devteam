# 1.4 Миграция: таблица `tasks`

**Sprint:** 1 — Новые модели данных и миграции
**Файл миграции:** `backend/db/migrations/017_create_tasks.sql`
**Go-модель:** `backend/internal/models/task.go`
**Статус:** ⬜ Не начата

---

## Описание

Создать таблицу `tasks` — основную единицу работы между агентами. Задачи создаются оркестратором или пользователем, назначаются агентам, проходят через pipeline статусов (planning → in_progress → review → testing → completed). Поддерживаются подзадачи (self-referencing FK), артефакты (diff, PR URL), привязка к git-веткам.

---

## Схема связей

```
tasks
  ├──> projects (FK: project_id, NOT NULL, ON DELETE CASCADE)
  ├──> tasks (FK: parent_task_id, nullable, ON DELETE SET NULL) — подзадачи
  ├──> agents (FK: assigned_agent_id, nullable, ON DELETE SET NULL)
  └──> users (FK: created_by_id при created_by_type='user')
       agents (FK: created_by_id при created_by_type='agent')
       * полиморфная связь — FK не на уровне БД, валидация в service-слое
```

---

## Таблица `tasks`

| Поле | Тип | Nullable | Default | Описание |
|------|-----|----------|---------|----------|
| `id` | UUID | NO | `gen_random_uuid()` | PK |
| `project_id` | UUID | NO | — | FK → projects ON DELETE CASCADE |
| `parent_task_id` | UUID | YES | — | FK → tasks ON DELETE SET NULL (подзадачи) |
| `title` | VARCHAR(500) | NO | — | Краткое название задачи |
| `description` | TEXT | NO | `''` | Подробное описание |
| `status` | VARCHAR(50) | NO | `'pending'` | Статус (см. таблицу ниже) |
| `priority` | VARCHAR(50) | NO | `'medium'` | `critical` / `high` / `medium` / `low` |
| `assigned_agent_id` | UUID | YES | — | FK → agents ON DELETE SET NULL |
| `created_by_type` | VARCHAR(50) | NO | — | `user` / `agent` |
| `created_by_id` | UUID | NO | — | ID создателя (полиморфная связь) |
| `context` | JSONB | NO | `'{}'` | Релевантный контекст из векторки |
| `result` | TEXT | YES | — | Результат выполнения |
| `artifacts` | JSONB | NO | `'{}'` | Артефакты (diff, test results, PR URL) |
| `branch_name` | VARCHAR(255) | YES | — | Git-ветка для задачи |
| `error_message` | TEXT | YES | — | Описание ошибки (если failed) |
| `started_at` | TIMESTAMPTZ | YES | — | Когда задача взята в работу |
| `completed_at` | TIMESTAMPTZ | YES | — | Когда задача завершена |
| `created_at` | TIMESTAMPTZ | NO | `NOW()` | |
| `updated_at` | TIMESTAMPTZ | NO | `NOW()` | |

---

## Статусы задачи

```
pending → planning → in_progress → review → testing → completed
                  ↘ failed    ↗ changes_requested ↗      ↘ failed
                    cancelled (пользователь остановил)
                    paused (пользователь приостановил)
```

| Статус | Описание |
|--------|----------|
| `pending` | Создана, ожидает назначения |
| `planning` | Планировщик декомпозирует задачу |
| `in_progress` | Разработчик пишет код |
| `review` | Ревьюер проверяет код |
| `changes_requested` | Ревьюер запросил изменения → возврат разработчику |
| `testing` | Тестировщик проверяет |
| `completed` | Готово |
| `failed` | Ошибка (с описанием в `error_message`) |
| `cancelled` | Отменена пользователем |
| `paused` | Приостановлена пользователем |

---

## Индексы

| Индекс | Тип | Колонки | Описание |
|--------|-----|---------|----------|
| `tasks_pkey` | PK | `id` | |
| `idx_tasks_project_id` | INDEX | `project_id` | Задачи проекта |
| `idx_tasks_parent_task_id` | INDEX | `parent_task_id` | Подзадачи |
| `idx_tasks_assigned_agent_id` | INDEX | `assigned_agent_id` | Задачи агента |
| `idx_tasks_status` | INDEX | `status` | Фильтр по статусу |
| `idx_tasks_project_status` | INDEX | `(project_id, status)` | Задачи проекта по статусу |
| `idx_tasks_priority` | INDEX | `priority` | Фильтр по приоритету |
| `idx_tasks_created_by` | INDEX | `(created_by_type, created_by_id)` | Задачи по создателю |
| `idx_tasks_branch_name` | INDEX | `branch_name` | Поиск задачи по ветке |

---

## CHECK constraints

| Constraint | Условие |
|------------|---------|
| `chk_tasks_status` | `status IN ('pending', 'planning', 'in_progress', 'review', 'changes_requested', 'testing', 'completed', 'failed', 'cancelled', 'paused')` |
| `chk_tasks_priority` | `priority IN ('critical', 'high', 'medium', 'low')` |
| `chk_tasks_created_by_type` | `created_by_type IN ('user', 'agent')` |

---

## Решение по полиморфной связи `created_by_id`

Поле `created_by_id` — полиморфная ссылка: может указывать на `users.id` или `agents.id` в зависимости от `created_by_type`. На уровне БД **не ставим FK** — валидация целостности выполняется в service-слое. Это стандартный подход для полиморфных связей в PostgreSQL (аналог: polymorphic associations в Rails/Django).

**Альтернатива:** два отдельных nullable FK (`created_by_user_id`, `created_by_agent_id`) с CHECK `(num_nonnulls(...) = 1)`. Отвергнута — усложняет запросы без значимого выигрыша для нашего use case.

---

## SQL миграция

### UP

```sql
-- +goose Up
-- +goose StatementBegin

CREATE TABLE tasks (
    id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id        UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    parent_task_id    UUID REFERENCES tasks(id) ON DELETE SET NULL,
    title             VARCHAR(500) NOT NULL,
    description       TEXT NOT NULL DEFAULT '',
    status            VARCHAR(50) NOT NULL DEFAULT 'pending',
    priority          VARCHAR(50) NOT NULL DEFAULT 'medium',
    assigned_agent_id UUID REFERENCES agents(id) ON DELETE SET NULL,
    created_by_type   VARCHAR(50) NOT NULL,
    created_by_id     UUID NOT NULL,
    context           JSONB NOT NULL DEFAULT '{}',
    result            TEXT,
    artifacts         JSONB NOT NULL DEFAULT '{}',
    branch_name       VARCHAR(255),
    error_message     TEXT,
    started_at        TIMESTAMP WITH TIME ZONE,
    completed_at      TIMESTAMP WITH TIME ZONE,
    created_at        TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at        TIMESTAMP WITH TIME ZONE DEFAULT NOW(),

    CONSTRAINT chk_tasks_status CHECK (status IN (
        'pending', 'planning', 'in_progress', 'review',
        'changes_requested', 'testing', 'completed',
        'failed', 'cancelled', 'paused'
    )),
    CONSTRAINT chk_tasks_priority CHECK (priority IN (
        'critical', 'high', 'medium', 'low'
    )),
    CONSTRAINT chk_tasks_created_by_type CHECK (created_by_type IN (
        'user', 'agent'
    ))
);

CREATE INDEX idx_tasks_project_id ON tasks(project_id);
CREATE INDEX idx_tasks_parent_task_id ON tasks(parent_task_id);
CREATE INDEX idx_tasks_assigned_agent_id ON tasks(assigned_agent_id);
CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_project_status ON tasks(project_id, status);
CREATE INDEX idx_tasks_priority ON tasks(priority);
CREATE INDEX idx_tasks_created_by ON tasks(created_by_type, created_by_id);
CREATE INDEX idx_tasks_branch_name ON tasks(branch_name) WHERE branch_name IS NOT NULL;

-- +goose StatementEnd
```

### DOWN

```sql
-- +goose Down
-- +goose StatementBegin

DROP INDEX IF EXISTS idx_tasks_branch_name;
DROP INDEX IF EXISTS idx_tasks_created_by;
DROP INDEX IF EXISTS idx_tasks_priority;
DROP INDEX IF EXISTS idx_tasks_project_status;
DROP INDEX IF EXISTS idx_tasks_status;
DROP INDEX IF EXISTS idx_tasks_assigned_agent_id;
DROP INDEX IF EXISTS idx_tasks_parent_task_id;
DROP INDEX IF EXISTS idx_tasks_project_id;
DROP TABLE IF EXISTS tasks;

-- +goose StatementEnd
```

---

## Go-модель

```go
package models

type TaskStatus string

const (
    TaskStatusPending          TaskStatus = "pending"
    TaskStatusPlanning         TaskStatus = "planning"
    TaskStatusInProgress       TaskStatus = "in_progress"
    TaskStatusReview           TaskStatus = "review"
    TaskStatusChangesRequested TaskStatus = "changes_requested"
    TaskStatusTesting          TaskStatus = "testing"
    TaskStatusCompleted        TaskStatus = "completed"
    TaskStatusFailed           TaskStatus = "failed"
    TaskStatusCancelled        TaskStatus = "cancelled"
    TaskStatusPaused           TaskStatus = "paused"
)

type TaskPriority string

const (
    TaskPriorityCritical TaskPriority = "critical"
    TaskPriorityHigh     TaskPriority = "high"
    TaskPriorityMedium   TaskPriority = "medium"
    TaskPriorityLow      TaskPriority = "low"
)

type CreatedByType string

const (
    CreatedByUser  CreatedByType = "user"
    CreatedByAgent CreatedByType = "agent"
)

type Task struct {
    ID              uuid.UUID      `gorm:"..." json:"id"`
    ProjectID       uuid.UUID      `gorm:"..." json:"project_id"`
    Project         *Project       `gorm:"foreignKey:ProjectID" json:"project,omitempty"`
    ParentTaskID    *uuid.UUID     `gorm:"..." json:"parent_task_id"`
    ParentTask      *Task          `gorm:"foreignKey:ParentTaskID" json:"parent_task,omitempty"`
    SubTasks        []Task         `gorm:"foreignKey:ParentTaskID" json:"sub_tasks,omitempty"`
    Title           string         `gorm:"..." json:"title"`
    Description     string         `gorm:"..." json:"description"`
    Status          TaskStatus     `gorm:"..." json:"status"`
    Priority        TaskPriority   `gorm:"..." json:"priority"`
    AssignedAgentID *uuid.UUID     `gorm:"..." json:"assigned_agent_id"`
    AssignedAgent   *Agent         `gorm:"foreignKey:AssignedAgentID" json:"assigned_agent,omitempty"`
    CreatedByType   CreatedByType  `gorm:"..." json:"created_by_type"`
    CreatedByID     uuid.UUID      `gorm:"..." json:"created_by_id"`
    Context         datatypes.JSON `gorm:"..." json:"context"`
    Result          *string        `gorm:"..." json:"result"`
    Artifacts       datatypes.JSON `gorm:"..." json:"artifacts"`
    BranchName      *string        `gorm:"..." json:"branch_name"`
    ErrorMessage    *string        `gorm:"..." json:"error_message"`
    StartedAt       *time.Time     `gorm:"..." json:"started_at"`
    CompletedAt     *time.Time     `gorm:"..." json:"completed_at"`
    CreatedAt       time.Time      `gorm:"..." json:"created_at"`
    UpdatedAt       time.Time      `gorm:"..." json:"updated_at"`
}
```

---

## Зависимости

**Требует:** projects (014), agents (004 + 016)
**Зависят от этой:** task_messages (018), conversations (019), Sprint 2+ (Task API)

---

## Критерии приёмки

- [ ] Файл `017_create_tasks.sql` создан
- [ ] UP и DOWN работают
- [ ] Тест UP → DOWN → UP проходит без ошибок
- [ ] Таблица `tasks` создана с 19 колонками
- [ ] FK: `project_id` → projects ON DELETE CASCADE
- [ ] FK: `parent_task_id` → tasks ON DELETE SET NULL (self-referencing)
- [ ] FK: `assigned_agent_id` → agents ON DELETE SET NULL
- [ ] CHECK: `status` (10 значений), `priority` (4 значения), `created_by_type` (2 значения)
- [ ] 9 индексов созданы (включая composite `project_id, status` и partial `branch_name`)
- [ ] Полиморфная связь `created_by_type` + `created_by_id` без FK (валидация в service)
- [ ] Go-модель `Task` создана с enum-типами `TaskStatus`, `TaskPriority`, `CreatedByType` и `IsValid()`
- [ ] Self-referencing relation: `ParentTask` + `SubTasks` через `ParentTaskID`
- [ ] `go build ./...` и `go test ./...` проходят
