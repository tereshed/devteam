# 2.11 MCP-инструменты: project_list, project_get, project_create

**Sprint:** 2 — CRUD API для Project и Team
**Файлы:**
- `backend/internal/mcp/tools_project.go` — инструменты проектов
- `backend/internal/mcp/tools_team.go` — инструменты команд
- `backend/internal/mcp/tools_project_test.go` — тесты проектов
- `backend/internal/mcp/tools_team_test.go` — тесты команд
- `backend/internal/mcp/mocks_test.go` — моки
- `backend/internal/mcp/server.go` — регистрация
**Статус:** ✅ Выполнена

---

## Описание

Ревизия и доработка MCP-инструментов для управления проектами и командами. Инструменты `project_list`, `project_get`, `team_get`, `team_update` **уже реализованы** (заглушки/ранние версии). Задача: добавить **`project_create`**, ревизировать существующие инструменты на соответствие финальным сигнатурам сервисов, убрать `nil`-guard'ы из `server.go`, расширить тесты.

---

## Текущее состояние

| Инструмент | Файл | Статус | Что нужно |
|------------|------|--------|-----------|
| `project_list` | `tools_project.go` | ✅ Реализован | Ревизия: проверить совпадение сигнатуры с `ProjectService.List` |
| `project_get` | `tools_project.go` | ✅ Реализован | Ревизия: аналогично |
| **`project_create`** | `tools_project.go` | ✅ Реализован | — |
| `team_get` | `tools_team.go` | ✅ Реализован | Ревизия |
| `team_update` | `tools_team.go` | ✅ Реализован | Ревизия |

---

## Новый инструмент: project_create

### Параметры

```go
type ProjectCreateParams struct {
    Name             string  `json:"name" jsonschema:"required,description=Название проекта"`
    Description      *string `json:"description,omitempty" jsonschema:"description=Описание проекта"`
    GitProvider      *string `json:"git_provider,omitempty" jsonschema:"description=Git-провайдер (local, github, gitlab, bitbucket)"`
    GitURL           *string `json:"git_url,omitempty" jsonschema:"description=URL git-репозитория"`
    GitDefaultBranch *string `json:"git_default_branch,omitempty" jsonschema:"description=Ветка по умолчанию (default: main)"`
}
```

**Примечание:** `git_credential_id`, `tech_stack`, `settings`, `vector_collection` не включены в MCP-параметры — это продвинутые настройки, которые удобнее конфигурировать через UI/API. MCP-инструмент предназначен для быстрого создания проекта из LLM-клиента.

### Handler

```go
func makeProjectCreateHandler(projectSvc service.ProjectService) func(ctx context.Context, req *mcp.CallToolRequest, params *ProjectCreateParams) (*mcp.CallToolResult, any, error) {
    return func(ctx context.Context, _ *mcp.CallToolRequest, params *ProjectCreateParams) (*mcp.CallToolResult, any, error) {
        if params == nil || params.Name == "" {
            return ValidationErr("name is required")
        }

        uid, ok := UserIDFromContext(ctx)
        if !ok {
            return ValidationErr("authentication required")
        }

        createReq := dto.CreateProjectRequest{
            Name: params.Name,
        }
        if params.Description != nil {
            createReq.Description = *params.Description
        }
        if params.GitProvider != nil {
            createReq.GitProvider = *params.GitProvider
        }
        if params.GitURL != nil {
            createReq.GitURL = *params.GitURL
        }
        if params.GitDefaultBranch != nil {
            createReq.GitDefaultBranch = *params.GitDefaultBranch
        }

        project, err := projectSvc.Create(ctx, uid, createReq)
        if err != nil {
            return projectServiceMCPError(err)
        }

        data := dto.ToProjectResponse(project)
        return OK(fmt.Sprintf("project %q created (id: %s)", data.Name, data.ID), data)
    }
}
```

### Регистрация (tools_project.go)

Добавить в `RegisterProjectTools`:

```go
mcp.AddTool(server, &mcp.Tool{
    Name:        "project_create",
    Description: "Создать новый проект. Автоматически создаёт команду с агентами по умолчанию. Как POST /projects.",
}, makeProjectCreateHandler(projectSvc))
```

---

## Ревизия существующих инструментов

### Чеклист

| # | Проверка | Описание |
|---|----------|----------|
| 1 | Сигнатуры сервисов | `ProjectService.List` принимает `(ctx, userID, userRole, ListProjectsRequest)` — проверить, что MCP handler передаёт все аргументы |
| 2 | `ProjectService.GetByID` | Принимает `(ctx, userID, userRole, projectID)` — `userRole` передаётся |
| 3 | Лимит пагинации | `project_list` ставит дефолты limit/offset **после** вызова сервиса (строки 74–83) — переместить **до** вызова, чтобы сервис получал нормализованные значения |
| 4 | `nil`-guard в `server.go` | `if deps.ProjectService != nil {` → убрать (Sprint 2 гарантирует наличие) |
| 5 | `nil`-guard для TeamService | `if deps.ProjectService != nil && deps.TeamService != nil {` → убрать |
| 6 | Маппинг ошибок | `projectServiceMCPError` — проверить, что покрывает все ошибки из `ProjectService` |

### Баг: пагинация в project_list

Текущий код `tools_project.go` (строки 69–86):

```go
// Сначала вызывает сервис с сырыми limit/offset
projects, total, err := projectSvc.List(ctx, uid, role, listReq)

// Потом нормализует — но сервис уже получил ненормализованные значения!
limit, offset := listReq.Limit, listReq.Offset
if limit <= 0 { limit = 20 }
```

**Исправление:** Нормализовать `listReq.Limit` и `listReq.Offset` **до** вызова `projectSvc.List`. Или убрать нормализацию (если Service сам ставит дефолты — см. задачу 2.4).

---

## server.go: убрать nil-guard'ы

**До:**

```go
if deps.ProjectService != nil {
    RegisterProjectTools(server, deps.ProjectService)
}
if deps.ProjectService != nil && deps.TeamService != nil {
    RegisterTeamTools(server, deps.ProjectService, deps.TeamService)
}
```

**После:**

```go
RegisterProjectTools(server, deps.ProjectService)
RegisterTeamTools(server, deps.ProjectService, deps.TeamService)
```

---

## Тестирование

### Новые тесты для project_create

**Файл:** `backend/internal/mcp/tools_project_test.go`

| # | Тест | Что проверяем |
|---|------|---------------|
| 1 | `TestProjectCreate_Success` | Успех → `Response.Status == "ok"`, data содержит project с name |
| 2 | `TestProjectCreate_MissingName` | Пустое имя → `ValidationErr` |
| 3 | `TestProjectCreate_NoAuth` | Нет userID в ctx → `ValidationErr("authentication required")` |
| 4 | `TestProjectCreate_DuplicateName` | `ErrProjectNameExists` → error response |
| 5 | `TestProjectCreate_WithOptionalFields` | Передать description + git_provider → корректно передаются в сервис |
| 6 | `TestProjectCreate_ServiceError` | Произвольная ошибка → error response |

### Существующие тесты — проверить актуальность

| Файл | Тестов | Проверить |
|------|--------|-----------|
| `tools_project_test.go` | 5 (List×3 + Get×3) | Сигнатуры моков совпадают с финальным сервисом |
| `tools_team_test.go` | 4 | Аналогично |

### Мок (mocks_test.go)

`mockProjectService.Create` **уже реализован** (строки 100–106). Новых моков не нужно.

### Запуск

```bash
cd backend && go test ./internal/mcp/ -v
```

---

## Итоговый список MCP-инструментов (Sprint 2)

| Инструмент | Описание | HTTP-аналог |
|------------|----------|-------------|
| `project_list` | Список проектов с пагинацией/фильтрацией | `GET /projects` |
| `project_get` | Получить проект по UUID | `GET /projects/:id` |
| `project_create` | Создать проект | `POST /projects` |
| `team_get` | Получить команду проекта с агентами | `GET /projects/:id/team` |
| `team_update` | Обновить команду (name) | `PUT /projects/:id/team` |

**Не добавляем в MCP (Sprint 2):**
- `project_update` — сложная логика partial update, множество полей; удобнее через UI
- `project_delete` — деструктивная операция, не для LLM-клиента
- Agent CRUD — Sprint 4

---

## Зависимости

**Требует:** `ProjectService` (2.4), `TeamService` (2.7), DTO (2.5)
**Используется в:** MCP-сервер (`cmd/api/main.go`)

---

## Критерии приёмки

- [x] `project_create` реализован и зарегистрирован в `RegisterProjectTools`
- [x] `server.go`: `nil`-guard'ы для ProjectService/TeamService убраны
- [x] Баг с пагинацией в `project_list` исправлен (нормализация **до** вызова сервиса)
- [x] Все сигнатуры MCP handler'ов совпадают с финальными интерфейсами сервисов
- [x] 6 новых тестов для `project_create`
- [x] Существующие тесты (5 project + 4 team) актуальны и проходят
- [x] `go build ./...` проходит
- [x] `go test ./internal/mcp/ -v` — все тесты зелёные
