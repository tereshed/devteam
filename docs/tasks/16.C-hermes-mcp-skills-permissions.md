# 16.C Hermes Agent — per-agent MCP / Skills / permissions

**Sprint:** 16 — Hermes Agent (Nous Research, MIT) как третий `code_backend`
**Фаза:** C (Customization). Завершает Sprint 16; A/B/D уже сделаны.
**Файлы:** `backend/internal/service/hermes_settings_service.go` (новый), `backend/internal/sandbox/docker_runner.go` (расширение `CopyToContainer`), `deployment/sandbox/hermes/entrypoint.sh` (новые фазы), `frontend/lib/features/team/presentation/widgets/agent_sandbox_settings_dialog.dart`, `frontend/lib/features/team/domain/models/agent_settings_model.dart`, новые fixtures
**Статус:** ⬜ Запланирована

---

## Описание

Включить per-agent кастомизацию Hermes Agent так же, как это сделано для Claude Code в Sprint 15.D: MCP-серверы, Skills, permissions / toolsets. После завершения C Hermes-агент перестаёт быть «всегда дефолтный» — пользователь выставляет:

- какие MCP-серверы доступны (с env-переменными для аутентификации);
- какие Hermes Skills предзагружены в сессию;
- какие toolsets разрешены (file ops / shell / web fetch / …) и какой permission mode (yolo / accept / plan / default);
- какие пер-агентные настройки `temperature` / `max_turns`.

Hermes конфиг живёт в `~/.hermes/config.yaml` + `~/.hermes/.env`, плюс есть `~/.hermes/skills/` и `~/.hermes/mcp.json`. Бэкенд должен сериализовать `agent.code_backend_settings` JSONB в эти файлы, runner — смонтировать их в контейнер до старта `hermes chat`. Резолвер уже выдаёт нужные env (фаза A); фаза C добавляет файловую часть.

---

## Контекст

После A/B/D:

- `agent.code_backend = 'hermes'` запускает `devteam/sandbox-hermes:local`.
- `agent.provider_kind` маршрутизирует на `OPENROUTER_API_KEY` / `ANTHROPIC_API_KEY` / etc.
- `agent.model` напрямую попадает в `hermes chat -m`.
- `agent.code_backend_settings` (JSONB) **игнорируется** — Hermes-агент работает только с дефолтными toolsets и без MCP/Skills.

Это блокер для production: Hermes мощен именно своей расширяемостью (40+ toolsets, agentskills.io-совместимые skills, любые MCP-серверы из ~/.hermes/mcp.json). Без C он эквивалентен «голому» `hermes chat -q` и не имеет преимущества над Claude Code.

---

## Зависимости

**Требует:**

- Sprint 16 A (`code_backend=hermes` в БД и enum'е).
- Sprint 16 B (sandbox-image + entrypoint).
- Sprint 15.21 (`AgentSettingsService` — общий контракт `code_backend_settings`).
- Sprint 15.22 (паттерн «runner кладёт файлы в `/workspace/.claude/` до старта»).

**Используется в:** UI agent_edit_dialog → расширенные настройки (Frontend 16.E, который сейчас уже потребляет `code_backend=hermes` в dropdown без своих опций).

---

## Архитектура и DRY

Hermes-конфиг **сильно** отличается от Claude Code'овского:

| Артефакт | Claude Code (15.22) | Hermes (16.C) |
|---|---|---|
| Settings | `~/.claude/settings.json` (`permissions.allow/deny/defaultMode`) | `~/.hermes/config.yaml` (`model`, `tools.toolsets`, `tools.allowlist`, `display.*`, …) |
| MCP | `.mcp.json` в корне репо или `~/.claude/mcp.json` | `~/.hermes/mcp.json` (другая схема) или `hermes mcp add ...` |
| Skills | `~/.claude/skills/<name>/` (директория) | `~/.hermes/skills/<name>/` (директория) — формат **agentskills.io** совместим |
| Permission flag | `--permission-mode acceptEdits` или `--dangerously-skip-permissions` | `--yolo` либо отсутствие = interactive prompt (в headless контейнере = ошибка) |
| Toolsets | `--allowedTools "Bash,Edit,..."` (просто строка) | `-t toolsets1,toolsets2` (Hermes toolsets — это профили из их каталога) |

DRY-возможности:

- `AgentSettingsService` Sprint 15.21 принимает общий `code_backend_settings` JSON и **сейчас** генерирует артефакты только для Claude Code. Расширяем его на Hermes: новый метод `BuildHermesArtifacts(agent) (configYAML, mcpJSON, skills, env, error)`. Существующий `BuildArtifacts` остаётся для Claude Code; диспатч по `agent.code_backend`.
- Skills, совместимые с agentskills.io, переносятся как есть — копируется директория `~/.claude/skills/foo/` → `~/.hermes/skills/foo/` без изменений. Если пользователь подписан на Skill один раз, он работает в обоих бэкендах.

---

## Контракт `code_backend_settings` для Hermes

Расширяем JSON Schema (валидация в `AgentSettingsService.Validate`):

```json
{
  "code_backend": "hermes",
  "hermes": {
    "toolsets": ["file_ops", "shell", "web_fetch"],
    "permission_mode": "yolo",
    "max_turns": 12,
    "temperature": 0.1,
    "skills": [
      { "name": "code-review-checklist", "source": "builtin" },
      { "name": "company-style", "source": "agentskills:org/style@v1.2" }
    ],
    "mcp_servers": [
      {
        "name": "github",
        "transport": "stdio",
        "command": "npx",
        "args": ["-y", "@modelcontextprotocol/server-github"],
        "env": {
          "GITHUB_PERSONAL_ACCESS_TOKEN": "${secret:github_pat}"
        }
      }
    ]
  }
}
```

Сегрегация полей:

- `toolsets` — белый список Hermes toolset-имен (валидируется по их каталогу). Дефолт: `["file_ops","shell"]`.
- `permission_mode` — enum `yolo|accept|plan|default`. Дефолт `yolo` (изолированный контейнер).
- `max_turns` — int 1..200. Дефолт 12 (соответствует Hermes-дефолту).
- `temperature` — float 0..2. Дефолт null (Hermes сам решит).
- `skills` — список объектов `{ name, source }`. `source` ∈ `builtin|agentskills|path`.
- `mcp_servers` — список объектов в схеме `~/.hermes/mcp.json`. Поддерживается substitution `${secret:NAME}` для секретов из `user_llm_credentials` или `agent_secrets`.

---

## Backend

### 1. `HermesSettingsService` (новый)

`backend/internal/service/hermes_settings_service.go`. Интерфейс:

```go
type HermesSettingsService interface {
    BuildArtifacts(ctx context.Context, agentID uuid.UUID) (*HermesArtifacts, error)
}

type HermesArtifacts struct {
    ConfigYAML []byte             // ~/.hermes/config.yaml
    MCPJSON    []byte             // ~/.hermes/mcp.json (опц.)
    Skills     map[string][]byte  // map[skillName/file] → содержимое
    Env        map[string]string  // секреты для MCP serve env (HERMES_MCP_*)
}
```

Реализация:

1. Лукап `agent.code_backend_settings` JSONB.
2. Валидация против JSON Schema.
3. Резолв `${secret:NAME}` → `user_llm_credentials.GetPlaintext(...)` или `agent_secrets`.
4. Сериализация:
   - `config.yaml` — YAML с полями `model`, `provider`, `tools.toolsets`, `display`, `terminal.timeout`.
   - `mcp.json` — массив MCP-серверов в их схеме.
   - `skills/` — копируется из реестра Skills (схема Sprint 15.23 + agentskills.io).
5. Возврат структуры. Caller (sandbox runner) сам решает, какие файлы куда положить.

### 2. Расширение `DockerSandboxRunner.RunTask` для Hermes

`backend/internal/sandbox/docker_runner.go`: после создания контейнера и до `ContainerStart` копируем артефакты через `CopyToContainer`. Сейчас аналогичная логика есть для Claude Code (`/workspace/.claude/settings.json`, `/workspace/.mcp.json`) — расширяем dispatch на Hermes:

| Артефакт | Целевой путь в контейнере |
|---|---|
| `ConfigYAML` | `/home/sandbox/.hermes/config.yaml` (chmod 0600) |
| `MCPJSON` | `/home/sandbox/.hermes/mcp.json` (chmod 0600) |
| `Skills` (директория) | `/home/sandbox/.hermes/skills/<name>/<file>` (chmod 0644 файлы, 0700 директории) |
| `Env` (MCP secrets) | в `SandboxOptions.EnvVars` (см. ValidateEnvKeys whitelist) |

Источник опций: `SandboxOptions.AgentArtifacts` (новое поле, опц.). Если nil — текущий путь.

### 3. Резолвер env для permission_mode и toolsets

`backend/internal/service/sandbox_auth_resolver.go` остаётся как есть. Permission/toolsets — это **не auth**, а агент-настройки, идут через `code_backend_settings`. Резолвер пробрасывает только провайдер-ключи (фаза A). Каскад:

- `code_backend_settings.hermes.permission_mode` → entrypoint смотрит env `DEVTEAM_HERMES_PERMISSION_MODE` → подставляет `--yolo` / `--accept-hooks` / опускает (для plan/default).
- `code_backend_settings.hermes.toolsets` → env `DEVTEAM_HERMES_TOOLSETS` (comma-separated) → entrypoint подставляет `-t "$DEVTEAM_HERMES_TOOLSETS"`.
- `code_backend_settings.hermes.skills` → entrypoint подставляет `-s "name1,name2"` ИЛИ `--skills` (см. `hermes chat --help`), плюс файлы в `~/.hermes/skills/`.
- `code_backend_settings.hermes.max_turns` → env `DEVTEAM_HERMES_MAX_TURNS` → `--max-turns N`.

ContextBuilder Sprint 15 уже формирует `EnvSecrets` map для `EnvVars`; добавляем новые ключи (нужно расширить whitelist в `ValidateEnvKeys`).

### 4. Entrypoint

`deployment/sandbox/hermes/entrypoint.sh`:

- Новая фаза `prepare_hermes_dotdir` (между clone и agent): проверяет наличие `/home/sandbox/.hermes/`, выставляет mode 0700, валидирует MCP-конфиг через `hermes mcp list` (быстрый sanity-check).
- Фаза `agent` обогащает аргументы:
  ```bash
  HERMES_TOOLSETS_ARGS=()
  [[ -n "${DEVTEAM_HERMES_TOOLSETS:-}" ]] && HERMES_TOOLSETS_ARGS+=("-t" "${DEVTEAM_HERMES_TOOLSETS}")

  HERMES_SKILLS_ARGS=()
  [[ -n "${DEVTEAM_HERMES_SKILLS:-}" ]] && HERMES_SKILLS_ARGS+=("-s" "${DEVTEAM_HERMES_SKILLS}")

  HERMES_PERM_ARGS=()
  case "${DEVTEAM_HERMES_PERMISSION_MODE:-yolo}" in
    yolo)   HERMES_PERM_ARGS+=("--yolo") ;;
    accept) HERMES_PERM_ARGS+=("--accept-hooks") ;;
    plan|default) : ;;   # default: интерактивный prompt — в headless контейнере падает
    *)      HERMES_PERM_ARGS+=("--yolo") ;;
  esac

  HERMES_MAX_TURNS_ARGS=()
  [[ -n "${DEVTEAM_HERMES_MAX_TURNS:-}" ]] && HERMES_MAX_TURNS_ARGS+=("--max-turns" "${DEVTEAM_HERMES_MAX_TURNS}")
  ```

### 5. Whitelist (validate.go)

Добавить:

- `DEVTEAM_HERMES_TOOLSETS`
- `DEVTEAM_HERMES_SKILLS`
- `DEVTEAM_HERMES_PERMISSION_MODE`
- `DEVTEAM_HERMES_MAX_TURNS`
- `HERMES_MCP_*` (префикс для MCP secrets — нужен wildcard, либо явные имена из реестра)

---

## Frontend

### 1. Модели

`frontend/lib/features/team/domain/models/agent_settings_model.dart` уже содержит `kSupportedCodeBackends = [..., 'hermes', ...]`. Расширить структуру `AgentSettingsModel` под Hermes-specific JSON:

```dart
@freezed
abstract class HermesSettings with _$HermesSettings {
  const factory HermesSettings({
    @Default(['file_ops','shell']) List<String> toolsets,
    @Default('yolo') String permissionMode,
    @Default(12) int maxTurns,
    double? temperature,
    @Default([]) List<HermesSkillRef> skills,
    @Default([]) List<MCPServerSpec> mcpServers,
  }) = _HermesSettings;
  factory HermesSettings.fromJson(Map<String,dynamic> json) => _$HermesSettingsFromJson(json);
}
```

`AgentSettingsModel.codeBackendSettings` остаётся `Map<String,dynamic>`; парсинг hermes-секции — отдельным помощником.

### 2. Диалог настроек

`frontend/lib/features/team/presentation/widgets/agent_sandbox_settings_dialog.dart`: вкладки текущего диалога рассчитаны на Claude Code (Provider tab, MCP tab JSON, Skills tab JSON, Permissions). Сделать UI condition'но:

- Если `agent.codeBackend == 'hermes'`:
  - Вкладка «Provider» отображает поля Hermes-специфики: dropdown toolsets (multi-select из каталога), dropdown permission_mode, slider max_turns, optional temperature.
  - Вкладка «MCP» — тот же JSON-textarea (схема MCPServerSpec универсальная).
  - Вкладка «Skills» — тот же JSON-textarea, но валидация под HermesSkillRef.
  - Вкладка «Permissions» — заменяется на «Toolsets» (Hermes не имеет per-tool allow/deny как Claude Code).
- Иначе — текущее поведение (Claude Code).

L10n: `agentSandboxSettingsTabToolsets`, `agentSandboxSettingsHermesPermLabel`, `agentSandboxSettingsHermesMaxTurnsLabel` (ru + en).

### 3. Каталог toolsets

`/api/v1/hermes/toolsets` — новая ручка, возвращает статичный список (захардкоден на бэке либо парсится из Hermes upstream). Используется dropdown'ом во frontend. MCP-инструмент опц.

---

## Тесты

### Backend

1. **Unit** `hermes_settings_service_test.go`:
   - Валидный полный JSON → корректные YAML/JSON/files.
   - `${secret:NAME}` → подстановка реального значения через mock-резолвер.
   - Неизвестный toolset → `ErrHermesInvalidToolset`.
   - `permission_mode=plan` в headless-режиме → warn (не error, но в логе).
2. **Unit** `docker_runner_test.go`:
   - Если `opts.AgentArtifacts != nil`, перед start копируются файлы в правильные пути с правильными permissions (mock Docker client).
3. **Integration** `hermes_settings_integration_test.go` (под `//go:build integration`):
   - Полный пайплайн: создаём агента с `code_backend=hermes` + skills+mcp; запускаем sandbox; проверяем `docker exec ls -la ~/.hermes/`.

### Frontend

1. **Widget** `agent_sandbox_settings_dialog_test.dart` (расширение):
   - `agent.codeBackend='hermes'` → вкладка Toolsets видна, Permissions скрыт.
   - Сохранение → правильный JSON в PUT-body.
2. **Unit** `hermes_settings_model_test.dart`:
   - Round-trip JSON (fromJson/toJson).
   - Дефолты применяются для пустых полей.

### E2E (`scripts/e2e_smoke.sh`)

Расширить tester-агент Phase D: вместо «голого» hermes — задать ему 1 MCP-сервер (например, mock `time-mcp`) и 1 Skill (`code-review-checklist`), проверить через `docker exec` контейнера, что файлы появились + Hermes их видит (`hermes mcp list` / `hermes skills`).

---

## Pitfalls и edge cases

1. **Hermes config.yaml vs CLI args**: некоторые опции (toolsets, model, provider) дублируются между config.yaml и `hermes chat` флагами. Контракт — **CLI args wins**, конфиг для defaults. MVP передаёт всё через флаги, конфиг минимален.
2. **Permission mode `plan`/`default`**: эти режимы требуют interactive prompt. В headless-контейнере Hermes повиснет → таймаут. Резолвер должен **отказать на сохранении** агента с такими режимами (валидатор `Validate`), либо принудительно подменить на `yolo` с предупреждением в логе.
3. **MCP secrets**: `${secret:NAME}` — это шаблон. Резолв должен идти ДО сериализации (плейн-текст в `mcp.json` в контейнере). Файл `mcp.json` — bytea volume, в логах не должен фигурировать (как и `settings.json` 15.37).
4. **Skill collision**: если skill_name из реестра совпадает с встроенным Hermes skill — приоритет user-supplied (наш файл перетирает). Документируем в JSON Schema.
5. **agentskills.io-источники**: при `source: "agentskills:org/style@v1.2"` нужен download-механизм с кэшем. MVP — не реализуем, валидатор отказывает; делается отдельной задачей 16.C.2.
6. **Hermes update**: запиненный `HERMES_REF` в Dockerfile — `v2026.5.7`. CLI флаги (`-t`, `-s`, `--yolo`, `--max-turns`) могут переименоваться в новых релизах. Перед бампом ARG — прогон e2e обязателен.
7. **Размер образа**: каждый skill — это файлы в контейнере. Если skill 50MB и таких 10 — образ распухает. Skills копируются **per-task** через `CopyToContainer`, не зашиваются в image — что и происходит.

---

## Готово, когда

- [ ] `HermesSettingsService.BuildArtifacts` написан, покрыт unit-тестами.
- [ ] `DockerSandboxRunner` копирует артефакты Hermes по правильным путям с правильными permissions.
- [ ] `validate.go` whitelist расширен (`DEVTEAM_HERMES_*`).
- [ ] `entrypoint.sh` Hermes понимает `DEVTEAM_HERMES_TOOLSETS/SKILLS/PERMISSION_MODE/MAX_TURNS`.
- [ ] `agent_sandbox_settings_dialog.dart` показывает Hermes-вкладки, когда `code_backend=hermes`. L10n ru+en.
- [ ] `e2e_smoke.sh` tester-агент использует MCP + Skill; sandbox-container показывает их через `docker exec`.
- [ ] `flutter analyze` чисто; `go test ./...` зелёный; `e2e_smoke.sh` → PR.
- [ ] Security-аудит: MCP secrets не в логах, `~/.hermes/*.json` — mode 0600.

---

## Оценка

~1 рабочий день при отсутствии сюрпризов в Hermes upstream-API. Распределение:

| Подзадача | Время |
|---|---|
| `HermesSettingsService` + JSON Schema + валидация | 2h |
| `DockerSandboxRunner` extension + tests | 1h |
| Entrypoint + whitelist + env-mapping | 1h |
| Frontend: модели + диалог + l10n | 2h |
| Тесты (unit + widget + integration) | 1.5h |
| E2E расширение + прогон + отладка | 1h |
