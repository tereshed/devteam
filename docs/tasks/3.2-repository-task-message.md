# 3.2 Repository: TaskMessageRepository

**Sprint:** 3 — CRUD API для Tasks
**Файл:** `backend/internal/repository/task_message_repository.go`
**Тест:** `backend/internal/repository/task_message_repository_test.go` (integration)
**Статус:** ✅ Выполнена

---

## Описание

Создать репозиторий для работы с сообщениями задач (`task_messages`). Таблица append-only: сообщения создаются и читаются, но не обновляются и не удаляются по одному (удаляются каскадно при удалении задачи). Основные сценарии: добавить сообщение от агента/пользователя, получить хронологический лог задачи, отфильтровать по типу сообщения или отправителю.

---

## Интерфейс

```go
type TaskMessageRepository interface {
    Create(ctx context.Context, msg *models.TaskMessage) error
    GetByID(ctx context.Context, id uuid.UUID) (*models.TaskMessage, error)
    ListByTaskID(ctx context.Context, taskID uuid.UUID, filter TaskMessageFilter) ([]models.TaskMessage, int64, error)
    ListBySender(ctx context.Context, senderType models.SenderType, senderID uuid.UUID, filter TaskMessageFilter) ([]models.TaskMessage, int64, error)
    CountByTaskID(ctx context.Context, taskID uuid.UUID) (int64, error)
}
```

---

## Методы

### Create

Добавляет сообщение в лог задачи. Append-only — основная операция записи.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `msg` | `*models.TaskMessage` | Заполненная модель (TaskID, SenderType, SenderID, Content, MessageType обязательны) |

Возвращает ошибку при:
- Невалидном `task_id` (FK violation) → `ErrTaskNotFound`
- Невалидном `sender_type` / `message_type` (CHECK violation) → обёрнутая ошибка

### GetByID

Получает одно сообщение по UUID. Возвращает `ErrTaskMessageNotFound` при отсутствии.

### ListByTaskID

Основной метод чтения — хронологический лог сообщений задачи с пагинацией и фильтрами. Сортировка по `created_at ASC` (от старых к новым — лог).

### ListBySender

Все сообщения конкретного отправителя (полезно для аудита: "что написал агент X"). Сортировка по `created_at DESC`.

### CountByTaskID

Быстрый подсчёт сообщений задачи (для UI — badge на карточке задачи).

---

## Фильтры

```go
type TaskMessageFilter struct {
    MessageType *models.MessageType // Фильтр по типу (instruction/result/question/feedback/error)
    SenderType  *models.SenderType  // Фильтр по типу отправителя (user/agent) — используется ТОЛЬКО в ListByTaskID
    Limit       int                 // Размер страницы (default ставит Service: 50)
    Offset      int                 // Смещение
}
```

**Дефолты пагинации:** устанавливает Service/Handler (как в задаче 2.1). Репозиторий дублирует нормализацию лимита (defense in depth), чтобы `GORM Limit(0)` никогда не снял лимит.

**Важно:** Поле `SenderType` в фильтре используется **только** в `ListByTaskID`. Метод `ListBySender` получает `senderType` и `senderID` как отдельные параметры и **игнорирует** `filter.SenderType` — в нём `filter` используется только для `MessageType`, `Limit`, `Offset`.

### Защита лимита (defense in depth)

```go
func normalizeTaskMessageListLimit(limit int) int {
    if limit <= 0 {
        return 50  // default для сообщений
    }
    if limit > 200 {
        return 200
    }
    return limit
}
```

---

## Кастомные ошибки

```go
var (
    ErrTaskMessageNotFound = errors.New("task message not found")
)
```

- `gorm.ErrRecordNotFound` → `ErrTaskMessageNotFound`
- FK violation на `task_id` → `ErrTaskNotFound` (**реимпорт** из `repository` пакета — ошибка определена в `task_repository.go`)

---

## Preload стратегия

Preload не используется — `task_messages` не имеет вложенных связей для загрузки. `Task` загружается отдельно в Service-слое при необходимости.

---

## Особенности реализации

### Append-only таблица

- **Нет метода `Update`** — сообщения иммутабельны после создания
- **Нет метода `Delete`** — удаление происходит каскадно через `ON DELETE CASCADE` при удалении задачи
- **Нет поля `updated_at`** — в миграции (018) у `task_messages` только `created_at`, что соответствует иммутабельности
- Это упрощает репозиторий и соответствует назначению таблицы (лог/аудит)

### Сортировка

| Метод | Сортировка | Причина |
|-------|-----------|---------|
| `ListByTaskID` | `created_at ASC` | Хронологический лог (от первого сообщения к последнему) |
| `ListBySender` | `created_at DESC` | Последние сообщения отправителя сверху |

Сортировка фиксирована в репозитории (не через фильтр) — для `task_messages` пользовательская сортировка не нужна, порядок определён семантикой.

---

## Зависимости

**Требует:** модель `TaskMessage` (задача 1.5), миграция 018 (задача 1.5), `TaskRepository` (задача 3.1 — для `ErrTaskNotFound`)
**Используется в:** `TaskService` (задача 3.3), `TaskHandler` (задача 3.5 — эндпоинты `GET/POST /tasks/:id/messages`), `RuleExtractorService` (Sprint 15 — извлечение правил из feedback-сообщений)

---

## Будущие расширения (Sprint 15 — Adaptive Learning)

`RuleExtractorService` будет анализировать `feedback`-сообщения для автоматического извлечения правил. Для этого может потребоваться кросс-задачный метод:

```go
ListByProject(ctx context.Context, projectID uuid.UUID, filter TaskMessageFilter) ([]models.TaskMessage, int64, error)
```

Реализация: `JOIN tasks ON task_messages.task_id = tasks.id WHERE tasks.project_id = ?`. Добавлять в Sprint 15, не сейчас.

---

## Конвенции

1. **Интерфейс + приватная struct** — `NewTaskMessageRepository(db *gorm.DB) TaskMessageRepository`
2. **`r.db.WithContext(ctx)`** — во всех запросах
3. **`fmt.Errorf("failed to ...: %w", err)`** — оборачивание ошибок
4. **Кастомные sentinel errors** — `ErrTaskMessageNotFound`
5. **Paging: `Count` + `Limit/Offset`** — аналогично `ProjectRepository`

---

## Тестирование

**Файл:** `backend/internal/repository/task_message_repository_test.go`
**Тег:** `//go:build integration`

### Тест-кейсы

| # | Тест | Что проверяем |
|---|------|---------------|
| 1 | `TestCreate_Success` | Создание сообщения, проверка всех полей включая metadata (JSON с tokens_used, model, duration_ms) |
| 2 | `TestCreate_InvalidTaskID` | Несуществующий `task_id` → FK violation / ошибка |
| 3 | `TestCreate_InvalidMessageType` | Невалидный `message_type` → CHECK violation |
| 4 | `TestCreate_InvalidSenderType` | Невалидный `sender_type` → CHECK violation |
| 5 | `TestGetByID_Success` | Получение по ID — все поля совпадают |
| 6 | `TestGetByID_NotFound` | Несуществующий UUID → `ErrTaskMessageNotFound` |
| 7 | `TestListByTaskID_Chronological` | Создать 5 сообщений → порядок `created_at ASC` |
| 8 | `TestListByTaskID_Pagination` | 10 сообщений, limit=3, offset=0 → 3 записи, count=10 |
| 9 | `TestListByTaskID_FilterByMessageType` | Фильтр `message_type=error` → только ошибки |
| 10 | `TestListByTaskID_FilterBySenderType` | Фильтр `sender_type=agent` → только агентские |
| 11 | `TestListBySender` | Два отправителя, фильтр по одному → только его сообщения |
| 12 | `TestCountByTaskID` | Создать N сообщений → count = N |
| 13 | `TestCascadeDelete` | Удалить задачу → все сообщения удалены (ListByTaskID пуст) |
| 14 | `TestIsolation_DifferentTasks` | Сообщения разных задач не смешиваются в ListByTaskID |
| 15 | `TestListByTaskID_LimitDefaults` | `Limit == 0` → не более 50 строк (defense in depth) |
| 16 | `TestListByTaskID_LimitCapped` | `Limit > 200` → не более 200 строк |
| 17 | `TestListBySender_IgnoresFilterSenderType` | `ListBySender(agent, id, filter{SenderType: user})` → фильтрует по agent, игнорирует filter.SenderType |

### Запуск

```bash
make test-integration
# или напрямую:
go test -tags=integration ./internal/repository/ -run TestTaskMessage -v
```

---

## Критерии приёмки

- [x] Файл `task_message_repository.go` создан
- [x] Интерфейс `TaskMessageRepository` определён (5 методов)
- [x] `TaskMessageFilter` struct для фильтрации
- [x] `Create` — создание + обработка FK/CHECK violations
- [x] `GetByID` — получение + обработка not found
- [x] `ListByTaskID` — пагинация + фильтры (MessageType, SenderType) + сортировка `created_at ASC`
- [x] `ListBySender` — пагинация + фильтр (только MessageType из filter) + сортировка `created_at DESC`
- [x] `CountByTaskID` — быстрый подсчёт
- [x] `normalizeTaskMessageListLimit` — defense in depth (default: 50, max: 200)
- [x] Нет методов Update/Delete (append-only)
- [x] Кастомные ошибки: `ErrTaskMessageNotFound`
- [x] `ErrTaskNotFound` — реимпорт из `task_repository.go` (не дублировать)
- [x] Конвенции: context, error wrapping, constructor pattern
- [x] Интеграционные тесты: 17 тест-кейсов (`//go:build integration`)
- [x] `go build ./...` проходит
- [ ] `make test-integration` проходит (нужен Yugabyte на `localhost:5433`)
