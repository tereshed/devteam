name: planner
role: planner
prompt_name: planner_prompt
model_config:
  temperature: 0.2
  # Phase 5 review: deepseek/deepseek-v4-flash через OpenRouter — см.
  # orchestrator.yaml для обоснования. Pairs с provider_kind="openrouter"
  # в записи агента (см. seed/SQL).
  model: "deepseek/deepseek-v4-flash"
is_active: true
